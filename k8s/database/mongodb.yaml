apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-healthcare-mongodb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smart-healthcare-mongodb
  template:
    metadata:
      labels:
        app: smart-healthcare-mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:latest
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongodb-storage
          mountPath: /data/db
      volumes:
      - name: mongodb-storage
        persistentVolumeClaim:
          claimName: mongodb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: smart-healthcare-mongodb
spec:
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    app: smart-healthcare-mongodb
